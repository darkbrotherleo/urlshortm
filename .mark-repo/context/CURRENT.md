# Current checkpoint

- Updated: 2026-08-28 00:00:00
- Completed: GĐ4 Nhân khẩu học (Meta): user_settings + demographic_snapshots (migration v10), MetaAudienceService (age/gender breakdown 90 ngày), SettingsController save/fetch/clear, tab demographics + bảng + biểu đồ trong Báo cáo, token che dấu (chỉ 4 ký tự cuối); lint 83, security scan PASS; E2E MySQL OK. Seed demo click_events cho link cũ chỉ có click_count legacy. Lưu IP thật (v11: click_events.ip_address, không mã hoá, toggle tracking.store_raw_ip; cột IP trong bảng + CSV). Preview tĩnh preview/ (portal index.html + landing + 10 tab dashboard + đăng nhập/đăng ký/trợ giúp + admin, assets tự chứa, tái tạo: php scripts/generate-preview.php + scripts/generate-admin-preview.php). Tài khoản: đổi mật khẩu + vô hiệu hoá (soft delete status=disabled, không xoá dữ liệu; login từ chối ACCOUNT_DISABLED). Hồ sơ chi tiết + hoá đơn (v12: phone/address/city/tax_type/company_name/tax_id/invoice_name, validate MST 10-14 số, form cài đặt 1 form, hiển thị ở Tài khoản); 126/126 PASS. ADMIN local: bảng admins (v13) + seed admin (darkbrotherleo@gmail.com, super_admin); /admin/dang-nhap + shell admin (render_admin_page dùng chung, users không submenu); Quản lý người dùng: /admin/users (bảng Username|Email|Gói|Ngày mua|Ngày hết hạn|Trạng thái) + click Username -> modal info + nút Sửa -> form full/gói/trạng thái (POST /admin/users/update); Quản lý gói (v14): mở rộng plans (price/currency/billing_period/max_*/has_*/is_popular) + upsert 4 gói (free/starter/pro/team, VND) + CRUD /admin/packages (list/search/paginate, new/edit form, toggle, delete chặn khi đang dùng, auto-slug từ tên). Tích hợp gói vào user: UserPlanService (planOf/usage/limits/canCreateLink/canAddDomain/canAddPixel/canClick/featureEnabled), chặn tạo link/pixel/domain + chặn click tháng theo giới hạn gói, hiển thị badge gói + panel "Gói & giới hạn" trong Tài khoản; Free seed giữ tính năng cơ bản (has_*=1, giới hạn 20 link/5 domain/5 pixel/10k click tháng); tài khoản mới mặc định gắn gói Free (subscribeToFreePlan khi register). Đơn hàng/thanh toán (v15: bảng orders + settings): menu admin "Cổng thanh toán" (/admin/payments, setup PayPal sandbox/live + mock), trang /thanh-toan (chọn gói + checkout), /thanh-toan/pay (PayPalService REST v2 hoặc mock), /thanh-toan/thanh-cong (kích hoạt gói ngay), /hoa-don/{code} (hoá đơn GTGT chuẩn VN, in được, xuất bằng chữ). Bảng giá public /bang-gia (4 gói + badge phổ biến + nút Mua ngay -> /thanh-toan?plan=X) + menu header "Bảng giá". Admin đơn hàng: /admin/orders (Danh sách đơn hàng: search/status/paginate, modal chi tiết, cập nhật trạng thái -> paid kích hoạt gói) + /admin/payment-history (Lịch sử thanh toán: đơn đã thanh toán, payer/paid_at). Trang tính năng /tinh-nang (landing bán hàng: hero, vấn đề, 8 feature card, vì sao chọn, testimonials, CTA + tích hợp chọn gói -> /thanh-toan?plan=X) + menu header "Tính năng". Admin Quản lý Link (/admin/links, chỉ mục chính): bảng User|URL|URL Short|Time Create|Time End|Click|QR Code|Action (Sửa modal + Vô hiệu/Hiện is_active, link vô hiệu bị chặn redirect); tìm theo username/slug/URL; tự xoá link khách (user_id NULL) không chỉnh sửa quá 15 ngày (v16 short_links.is_active). Quản lý Voucher (/admin/vouchers, chỉ mục chính): bảng Mã Voucher|Đơn hàng áp dụng|Giá gói|Giá giảm sau voucher|Trạng thái áp dụng|Action (Chạy/Ngừng), modal tạo/sửa (chiến dịch, mã, số lần, per_user once/multiple, %/tiền, ngày, note) + áp dụng voucher ở checkout (giảm giá + ghi nhận usage) (v17 vouchers + voucher_usages). Quản lý Domain (/admin/domains, chỉ mục chính): Domain Hệ Thống (thêm/bật-tắt/xoá/đặt mặc định — system_domains v18; link rút gọn dùng domain mặc định qua system_short_base, localhost Laragon urlshortm.test -> http, online -> https) + Domain của Users (bảng Username|Domain|Ngày thêm|Số lượng active/remaining|Trạng thái Hoạt động/Tạm dừng/Lỗi kết nối|Action Tạm dừng/Xoá). Bỏ cấu hình URL hệ thống (system_url); hệ thống tự nhận diện domain đang chạy cho toàn hệ thống; Cài đặt Website có 6 submenu: Thông tin hệ thống (read-only: tên miền/giao thức/base/máy chủ/DB/trạng thái), Thông tin website (tên/giới thiệu/logo/favicon), Hoá đơn (người bán + MST), Email SMTP, Media (định dạng tích chọn -> chặn upload không cho phép, nén ảnh, chuyển đổi WebP/AVIF, quản lý media + thumbnail 100x100), SEO (cơ bản/OG/verification/analytics/custom code/AI meta) + inject meta/script ra trang công khai (v19 media). FIX/Redesign: media/logo img tuyệt đối, Media/SEO compact, Robots Meta select, og value, sitemap.xml + robots.txt tự tạo, layout chuyên nghiệp 6 trang Cài đặt website (.a-settings-card/.a-field stacked, hệ thống overview table, 2 cột đều), SEO dạng bảng 2 cột + icon "?" tooltip hướng dẫn từng trường + trường nhập nội dung robots.txt (điền sẵn), áp dụng toàn bộ SEO lên website (site title, meta, OG, verification, GA4/GTM/pixel/tiktok/indexnow, custom code, hreflang, AI meta — verified 30 tag qua smoke), áp dụng thông tin "Hoá đơn" vào hoá đơn (người bán: tên/loại/MST/địa chỉ/điện thoại từ cài đặt website). Email SMTP: Mailer (SMTP client thuần PHP, STARTTLS 587/SSL 465, AUTH LOGIN, dot-stuffing) + nút "Gửi thử email" trên trang SMTP (test_to/subject/body, báo lỗi rõ khi chưa cấu hình); Email Template: khung HTML inline-CSS + 5 mau (mua thanh cong, hoa don, dang ky, lay lai mat khau, kich hoat) /admin/emails (preview iframe + gui thu), Mailer HTML, gui email mua+hoa don+sau dang ky (graceful khi chua cau hinh SMTP); 185/185 PASS
- In progress: Không
- v20 (email activation + password reset): register tạo user PENDING + activation_token 24h + gửi email kích hoạt (KHÔNG tự đăng nhập), /kich-hoat kích hoạt + tự đăng nhập, /quen-mat-khau -> reset_token 30 phút -> /dat-lai-mat-khau (1 lần dùng, hết hạn bị từ chối), login từ chối pending "chưa được kích hoạt", "Quên mật khẩu?" ở trang đăng nhập, admin users badge "Chờ kích hoạt", email template kich_hoat/lay_lai_mat_khau; migration v20 users.status ENUM active/pending/disabled + 4 cột token; smoke helper register_and_activate (đổi toàn bộ site đăng ký cũ); 187/187 PASS
- Exact next action: Xem Báo cáo: đăng nhập user 2 (nguyenphatseo@gmail.com) → tab Báo cáo → chọn "Tất cả link" hoặc link cụ thể; link youtube có mật khẩu phải unlock rồi mới track; GĐ5 tracking/retention khi cần
- Active plan: plans/019-email-activation-password-reset.md
- Blockers: Không
- Latest verification: PASS: full 187/187 (thêm smoke quên mật khẩu -> reset token 30 phút, integration register PENDING/login từ chối pending), lint 145 files, security scan, E2E MySQL migrate v20 (users.status enum + 4 cột token xác nhận)
- Risks to watch: Token Meta tuyệt đối không render vào HTML (chỉ 4 ký tự cuối); fetch ngoài mạng timeout 20s; SMTP/PayPal thật cần creds để E2E live
